# Optional process environment variables (Freon does not auto-load this file).
# Set only when your LM Studio server requires bearer authentication:
# FREON_LM_TOKEN=your-local-token

# Optional separate data directory for a demo or test workspace:
# FREON_DATA_DIR=/absolute/path/to/freon-data
